export enum Errors{
  SERVER_ERR0R = 500,
  INVALID_REQUEST = 400,
  INVALID_CREDENTIALS = 401,
  USER_ALREADY_EXISTS = 409,
  ACCESS_DENIED = 403,
  NOT_FOUND = 404,
  REQUIRED_FIELD = 401,
  SUCCESS = 200,
}